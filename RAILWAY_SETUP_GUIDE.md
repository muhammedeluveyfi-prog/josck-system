# 🚂 دليل إعداد Railway - خطوة بخطوة

## ✅ المشروع جاهز على GitHub
Repository: `https://github.com/muhammedeluveyfi-prog/josck-system`

---

## 📋 خطوات الإعداد في Railway

### الخطوة 1: إنشاء مشروع جديد

1. اذهب إلى [railway.app](https://railway.app)
2. اضغط **New Project**
3. اختر **Deploy from GitHub repo**
4. سجّل دخول GitHub إذا طُلب
5. اختر repository: **muhammedeluveyfi-prog/josck-system**
6. اضغط **Deploy Now**

---

### الخطوة 2: إعداد Root Directory (مهم جداً!)

بعد أن يبدأ Railway في البناء:

1. اضغط على **المشروع** (Project)
2. اضغط على **Settings** (الإعدادات)
3. ابحث عن قسم **Root Directory**
4. في حقل **Root Directory** أدخل:
   ```
   backend
   ```
5. اضغط **Save** أو **Update**

> ⚠️ **مهم**: بدون تحديد Root Directory، Railway لن يعرف أين ملف `package.json` للسيرفر!

---

### الخطوة 3: التحقق من Start Command

1. في نفس صفحة **Settings**
2. ابحث عن **Start Command**
3. يجب أن يكون:
   ```
   npm start
   ```
   (أو اتركه فارغاً - سيستخدم `npm start` تلقائياً)

---

### الخطوة 4: الحصول على رابط السيرفر

1. اذهب إلى **Settings** → **Networking**
2. اضغط **Generate Domain**
3. ستحصل على رابط مثل:
   ```
   https://josck-system-production.up.railway.app
   ```
4. **احفظ هذا الرابط!** 🎯

---

### الخطوة 5: اختبار السيرفر

افتح المتصفح واذهب إلى:
```
https://your-app-name.up.railway.app/api/health
```

يجب أن ترى:
```json
{
  "status": "ok",
  "message": "Server is running"
}
```

---

## 🔍 أين تجد Root Directory في Railway؟

### الطريقة 1: من صفحة المشروع
1. اضغط على **المشروع**
2. اضغط على **Settings** (في القائمة الجانبية)
3. ابحث عن **Root Directory** في قسم **Build**

### الطريقة 2: من Service Settings
1. اضغط على **Service** (الخدمة)
2. اضغط على **Settings**
3. ابحث عن **Root Directory**

---

## 📸 صورة توضيحية للإعدادات:

```
┌─────────────────────────────────────┐
│  Railway Project Settings           │
├─────────────────────────────────────┤
│                                     │
│  Root Directory: [backend    ]     │  ← هنا!
│                                     │
│  Start Command:  [npm start  ]     │
│                                     │
│  [Save] [Cancel]                    │
└─────────────────────────────────────┘
```

---

## ⚙️ متغيرات البيئة (اختياري)

في **Settings** → **Variables** يمكنك إضافة:

```
NODE_ENV=production
JWT_SECRET=your-secret-key-here
PORT=3000
```

> ملاحظة: Railway يحدد PORT تلقائياً - لا حاجة لإعداده يدوياً

---

## 🐛 استكشاف الأخطاء

### المشكلة: Build فشل
- ✅ تأكد من أن **Root Directory** = `backend`
- ✅ تأكد من أن `package.json` موجود في `backend/`
- ✅ تحقق من **Logs** لرؤية الخطأ

### المشكلة: السيرفر لا يعمل
- ✅ تحقق من **Logs** في Railway
- ✅ تأكد من أن `npm start` يعمل محلياً
- ✅ تحقق من أن جميع المكتبات مثبتة

### المشكلة: CORS Error
- ✅ تأكد من أن CORS في `server-local.js` يسمح بنطاق Frontend
- ✅ تم إعداد CORS للسماح بجميع النطاقات في الإنتاج

---

## ✅ قائمة التحقق

- [ ] تم رفع المشروع على GitHub
- [ ] تم إنشاء مشروع في Railway
- [ ] تم ربط GitHub repository مع Railway
- [ ] تم تحديد **Root Directory** = `backend`
- [ ] تم الحصول على رابط السيرفر
- [ ] تم اختبار السيرفر (`/api/health`)

---

## 🎯 الخطوة التالية

بعد الحصول على رابط Backend:
1. ارفع Frontend على **Vercel** أو **Netlify**
2. أضف متغير البيئة:
   ```
   VITE_API_URL=https://your-backend.up.railway.app/api
   ```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من **Logs** في Railway
2. تأكد من أن جميع الخطوات تمت بشكل صحيح
3. راجع ملف `backend/RAILWAY_DEPLOY.md` للتفاصيل الكاملة

---

**جاهز للبدء! 🚀**


